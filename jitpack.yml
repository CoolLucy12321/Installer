jdk:
  - openjdk8

before_install:
  - bash ensure-jitpack-java-8 install

install:
  - if ! bash ensure-jitpack-java-8 use; then source ~/.sdkman/bin/sdkman-init.sh; fi
  - java -version
  - ./gradlew publishToMavenLocal
